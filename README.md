"# Voice_AI_Agent_Nova"

cp [.env.example](http://_vscodecontentref_/0) .env → fill in AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, POLYGON_API_KEY
pip install -r requirements.txt
python main.py — server starts on localhost:8000
Check /health to confirm all 4 tools are registered
